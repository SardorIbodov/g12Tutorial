import React, { useState, useEffect, useRef } from "react";

export const UseRef = () => {
  // console.log("Ishladi");
  // const [name, setName] = useState("");
  // const countRef = useRef(1);

  // const showRender = () => {
  // 	setCount(count + 1)
  // };

  // useEffect(() => {
  // 	countRef.current = countRef.current + 1;
  // }, [name])

  // useEffect(() => {
  //   let h1 = document.querySelector("h1");
  //   console.log(h1);
  // });

	// const show = () => {
	// 	console.log(titleRef.current);
	// 	titleRef.current.style.transform = "translateX(20px)";
	// }

  return (
    <div>
      {/* <h1 ref={titleRef}>UseRef</h1>
			<button>Show</button> */}
      {/* <input
        type="text"
        placeholder="Type"
      /> */}
      {/* <h2>My name is {name}</h2> */}
      {/* <h2>{countRef.current} marta render bo'ldi</h2> */}
    </div>
  );
};

export default UseRef;
